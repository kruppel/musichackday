var nest = nest.nest('FQVXQRSHUQNJQILM3');

/**
 * OBJECTIVES
 *
 * 1. Create a catalog if not already done.
 *   a. Store the id for the catalog
 * 2. Playlists
 */

(function() {
  if (typeof(songbird) != 'undefined') {
  }
})();
